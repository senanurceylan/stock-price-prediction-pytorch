# Stock Price Prediction with PyTorch

An AI-powered stock price prediction project that uses historical market data and a PyTorch LSTM model to forecast future stock prices.

The project will gradually include data preprocessing, model training and evaluation, interactive visualizations, Redis caching, Azure integration, and AI-assisted prediction explanations.
